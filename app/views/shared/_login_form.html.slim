- if flash.alert
  p.alert = flash.alert

h2.login_title ログイン

.login_form
  = form_with url: :session, id: "login_form", local: true do |f|

    .form
      = f.label :name
      = f.text_field :name

    .form
      = f.label :password
      = f.password_field :password

    = f.submit "ログイン"